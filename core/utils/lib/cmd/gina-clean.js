var clean;
//var utils = require('gina').utils;
//var util = require('util');

clean = function(project, bundle){


    var init = function(project, bundle){

    };

    var clean = function(){

    };

    init(project, bundle);
//Later for the Web UI.
//    return {
//        onComplete : function(err){
//
//            init(project);
//        }
//    }
};

module.exports = clean;