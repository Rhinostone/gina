/**
 * My_bundle bundle
 *
 * */
var my_bundle = require('geena');
//my_bundle.onInitialize( function(event, app, express) {
//    //do whatever things you need to do before server starts
//    // eg.: app.use( express.cookieParser() );
//
//    //then notify the server that startup sequence can be resumed
//    event.emit('complete', app);
//});
my_bundle.start();