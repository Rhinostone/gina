/**
 * @module gina/lib/cmd/image/man
 * @description Renders the image group man page (falls back to the group
 * help.txt while no `gina-image.1.md` exists). Shared engine: `../man-render`.
 */
module.exports = require('../man-render');
