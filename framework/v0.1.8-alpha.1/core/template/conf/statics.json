{
  //"favicon.ico": "${}/favicon.ico",
  //"favicons": "${}/favicons",
  "html": "${}/html",  // used for templating
  "sass": "${}/sass",  // used for templating
  "handlers": "${}",  // used for templating
// TODO - what if the user add "js/vendor" ? Do we override of merge ?
  "css/vendor/gina": "${}/framework/v${}/core/asset/plugin/dist/vendor/gina/css",
  "js/vendor/gina": "${}/framework/v${}/core/asset/plugin/dist/vendor/gina/js",
  // for plain html or public access
  "": "${}"
}