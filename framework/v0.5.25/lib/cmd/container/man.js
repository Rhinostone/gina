/**
 * @module gina/lib/cmd/container/man
 * @description Renders the container group man page (falls back to the group
 * help.txt while no `gina-container.1.md` exists). Shared engine: `../man-render`.
 */
module.exports = require('../man-render');
