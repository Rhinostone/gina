kind: Fixed
body: 'CLI help text now shows the correct `--` flag prefix (previously rendered with an em-dash) for `gina --version` and the `project:add` / `project:import` `--path` argument.'
time: 2026-07-03T15:45:00+01:00
