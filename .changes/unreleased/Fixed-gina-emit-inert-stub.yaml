kind: Fixed
body: 'Calling `gina.emit(''error'', ...)` on the module object no longer throws — `emit` is now an inert stub that always returns `false` (the detached-copy assignment it replaces never dispatched to any listener; the module object is not an event surface — application events go through the controller''s `self.emitEvent()`)'
time: 2026-07-16T18:22:12+01:00
