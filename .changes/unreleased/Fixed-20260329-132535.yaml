kind: Fixed
body: 'Fixed entity method concurrency: concurrent callers on the same method now each receive their own result (FIFO queue dispatch replaces single-slot _callbacks; DISPATCH:PREEMPTIVE_BUFFER and consume paths queue-aware)'
time: 2026-03-29T13:25:35.168839+01:00
