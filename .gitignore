# IDE
.lh
.jshintrc
.idea
nbproject
.vscode
.history
*.codekit3

# SCM
.svn
.git

# Local-only — never track (prefix globs cover backup/variant filenames too)
.claude*
CLAUDE*

# Gina
.*
!/.gitignore
!/.npmignore
!/.changes
!/.github
!/.githooks
/services
/logs
/tmp
/cache/*
.gna
node_modules/
VERSION
MIDDLEWARE
# package-lock.json is tracked — required for reproducible builds, see
# bundle-freshness.yml which uses `npm ci` to avoid devDep drift baking
# different bytes into the published bundle (engine.io-client incident,
# 2026-04-24, llms.txt §72).
* copy
test/workspace
SUCCESS

# Desktop
.DS_Store
._.DS_Store
Icon?
desktop.ini
Thumbs.db

# Misc
/data
/cgi-bin
.env
*.bak
*.bak.*
*.old
*.old.js
src.old
*.log
!test/**/fixtures/**/*.log
*.afdesign

# Framework
framework/v*/package-lock.json
framework/v*/core/deps/*/package.json.dist
# framework/v*/
framework/v*/test/workspace
*.jar
*.map