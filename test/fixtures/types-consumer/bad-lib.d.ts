/**
 * Deliberately-broken declaration file — part of the CAN-FAIL control.
 *
 * The type below does not exist, so checking THIS FILE yields TS2304.
 * That diagnostic only surfaces when `skipLibCheck` is `false`; the driver
 * asserts its presence to prove the gate actually checks declaration files.
 */

declare const dto3bBadLibProbe: DefinitelyNotARealTypeNameForTheControl;
