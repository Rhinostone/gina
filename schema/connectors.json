{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://gina.io/schema/connectors.json",
  "title": "Gina connectors.json",
  "description": "Database and service connector declarations for a Gina bundle. Each key is a logical connector name; the value describes the connection parameters. Acquired at runtime via getConnection(). The required fields depend on the connector type.",
  "type": "object",
  "additionalProperties": {
    "$ref": "#/definitions/connector"
  },
  "definitions": {
    "connector": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "connector": {
          "type": "string",
          "description": "Connector driver type. Required when the key name does not match a built-in connector name (e.g. key \"session\" with connector \"redis\").",
          "enum": ["couchbase", "mysql", "postgresql", "sqlite", "redis", "ai", "scylladb", "mongodb"]
        },
        "version": {
          "type": "string",
          "description": "Optional semver range for the driver npm package. Used by `connector:list` to flag cross-bundle version-pin disagreements and by `connector:add` to print the exact install command. When omitted, the framework falls back to the project's package.json dependency range, then to the framework's built-in driver range table.",
          "examples": ["^5.0.0", ">=5.3.0 <6.0.0", "5.4.1"]
        },
        "protocol": {
          "type": "string",
          "description": "Connection protocol URI scheme. Required for Couchbase and AI connectors.",
          "examples": ["couchbase://", "mysql://", "postgres://", "redis://", "anthropic://", "openai://"]
        },
        "host": {
          "type": "string",
          "description": "Hostname or IP address. Supports comma-separated values for clusters and ${variable} interpolation. Required for Couchbase, MySQL, PostgreSQL, Redis.",
          "examples": ["127.0.0.1", "db1.example.com,db2.example.com"]
        },
        "port": {
          "type": ["number", "string"],
          "description": "Server port. Defaults: MySQL 3306, PostgreSQL 5432, Redis 6379.",
          "examples": [3306, 5432, 6379]
        },
        "database": {
          "type": "string",
          "description": "Database, bucket, or keyspace name. Also used as the model directory name under models/. Required for Couchbase, MySQL, PostgreSQL, SQLite, ScyllaDB."
        },
        "contactPoints": {
          "type": ["array", "string"],
          "description": "ScyllaDB / Cassandra only. Cluster contact points. Accepts an array of `host:port` strings, a comma-separated string, or falls back to the `host`+`port` pair when omitted. Defaults to [\"127.0.0.1:9042\"].",
          "examples": [["10.0.0.1:9042", "10.0.0.2:9042"], "node1:9042,node2:9042"]
        },
        "localDataCenter": {
          "type": "string",
          "description": "ScyllaDB / Cassandra only. Data center name used for token-aware routing. Required by cassandra-driver 4.x. Defaults to \"datacenter1\".",
          "examples": ["datacenter1", "us-east-1"]
        },
        "keyspace": {
          "type": "string",
          "description": "ScyllaDB / Cassandra only. CQL keyspace name. Alias for `database` when both are supported; `keyspace` wins when both are set.",
          "examples": ["mykeyspace"]
        },
        "credentials": {
          "type": "object",
          "description": "ScyllaDB / Cassandra only. Authentication credentials. Use this OR the legacy `username`+`password` pair.",
          "properties": {
            "username": { "type": "string" },
            "password": { "type": "string" }
          }
        },
        "username": {
          "type": "string",
          "description": "Authentication username."
        },
        "password": {
          "type": "string",
          "description": "Authentication password. Supports ${variable} interpolation for secrets."
        },
        "scope": {
          "type": "string",
          "description": "Data isolation scope stamped on every document at insert time and used to filter queries. Falls back to NODE_SCOPE env var when omitted.",
          "enum": ["local", "beta", "production", "testing"]
        },
        "connectionLimit": {
          "type": "number",
          "description": "Maximum connections in the pool. MySQL and PostgreSQL only. Defaults to 10.",
          "default": 10
        },
        "ssl": {
          "type": ["object", "boolean"],
          "description": "TLS/SSL configuration. Passed through to the underlying driver (mysql2, pg). Set to true for default TLS.",
          "additionalProperties": true
        },
        "file": {
          "type": "string",
          "description": "SQLite only. Database file path. Defaults to ~/.gina/{version}/{database}.sqlite. Use \":memory:\" for in-memory databases.",
          "examples": [":memory:", "/data/app.sqlite"]
        },
        "idleTimeout": {
          "type": "number",
          "description": "PostgreSQL only. Idle client timeout in milliseconds. Defaults to 30000.",
          "default": 30000
        },
        "connectionTimeout": {
          "type": "number",
          "description": "PostgreSQL only. Connection timeout in milliseconds. Defaults to 2000.",
          "default": 2000
        },
        "db": {
          "type": "number",
          "description": "Redis only. Database index (0-15). Defaults to 0.",
          "default": 0
        },
        "tls": {
          "type": "boolean",
          "description": "Redis only. Enable TLS for the connection."
        },
        "prefix": {
          "type": "string",
          "description": "Redis and SQLite session stores. Key prefix for session store entries. Defaults to \"sess:\". The Couchbase stores read `prefix` as a constructor option only, never from this file.",
          "default": "sess:"
        },
        "ttl": {
          "type": "number",
          "description": "Redis, SQLite, MongoDB and ScyllaDB session stores. Default TTL in seconds for session entries. When omitted, the session record follows the cookie's `maxAge`, falling back to 86400 only when the cookie has none. The Couchbase stores read `ttl` as a constructor option only, never from this file."
        },
        "cluster": {
          "type": "array",
          "description": "Redis Cluster only. Array of cluster node endpoints.",
          "items": {
            "type": "object",
            "required": ["host", "port"],
            "properties": {
              "host": { "type": "string" },
              "port": { "type": "number" }
            }
          }
        },
        "keepAlive": {
          "type": "boolean",
          "description": "Couchbase only. Enable TCP keep-alive. Defaults to true.",
          "default": true
        },
        "pingInterval": {
          "type": "string",
          "description": "Couchbase only. Interval to keep the connection alive (e.g. \"2m\", \"30s\").",
          "examples": ["2m", "30s", "1h"]
        },
        "useScopeAndCollections": {
          "type": "boolean",
          "description": "Couchbase only. Enable scope and collection support (SDK v3+). Defaults to true.",
          "default": true
        },
        "timeouts": {
          "type": "object",
          "description": "Couchbase only. Timeout overrides passed to the SDK.",
          "additionalProperties": { "type": "number" },
          "examples": [{ "kvTimeout": 10000 }]
        },
        "detailed_errcodes": {
          "type": "boolean",
          "description": "Couchbase only. Expose detailed error codes instead of generic messages."
        },
        "apiKey": {
          "type": "string",
          "description": "AI connector only. API key for the provider. Supports ${ENV_VAR} interpolation.",
          "examples": ["${ANTHROPIC_API_KEY}", "${OPENAI_API_KEY}"]
        },
        "model": {
          "type": "string",
          "description": "AI connector only. Default model identifier.",
          "examples": ["claude-sonnet-4-20250514", "gpt-4o", "deepseek-chat"]
        },
        "baseURL": {
          "type": "string",
          "description": "AI connector only. Override the default API base URL (OpenAI-compatible providers)."
        },
        "uri": {
          "type": "string",
          "description": "MongoDB only. Full connection string (mongodb:// or mongodb+srv://). Takes precedence over the decomposed host/port/username/password/authSource/replicaSet fields. The `database` field is still required and is used to select the db on the client.",
          "examples": ["mongodb://user:pass@localhost:27017/mydb?authSource=admin", "mongodb+srv://user:pass@cluster.mongodb.net/mydb"]
        },
        "authSource": {
          "type": "string",
          "description": "MongoDB only. Authentication database name (typically \"admin\"). Used when `uri` is absent and the username/password live under a different db than the one you're querying.",
          "examples": ["admin"]
        },
        "replicaSet": {
          "type": "string",
          "description": "MongoDB only. Replica set name. Used when `uri` is absent and you connect to a replica-set deployment.",
          "examples": ["rs0"]
        }
      }
    }
  }
}
